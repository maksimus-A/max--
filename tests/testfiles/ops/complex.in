fn add(x: int, y: int): int {
    return x + y;
}

fn main(): int {

    int x = 1;
    int y = 2;
    int z = (x + 1)-(y*2) + (x-2*y) / (2-x) + 1; // Another comment
    // This is a comment

    return 0;
}