fn add(int x, int y): int {
    return x + y;
}

fn inc(int x): int {
    return x+1;
}

fn one() {
    return 1;
}

fn main(): int {
    int x = 1;
    int y = 2;
    int z = add(x, y) + add(inc(inc(x)), one()) * 2;

    return z;
}

