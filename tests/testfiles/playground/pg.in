fn add(int x, int y): int {
    return x + y;
}

fn inc(int x): int {
    return x+1;
}

fn one(): int {
    return 1;
}

fn fake(): void {

}

fn main(): int {
    int x = 1;
    int y = 2;
    fake();
    int z = add(x, y) + add(inc(inc(x)), one()) * 2;

    return z;
}

