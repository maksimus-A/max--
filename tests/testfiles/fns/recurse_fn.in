// Recursive function call test.
fn sumUpToN(int n): int {
    if (n == 1) {return 1;}

    return n + sumUpToN(n-1);
}

fn main(): int {
    print(sumUpToN(20));
    return 0;
}
//CHECK_FILE:recurse_fn.out